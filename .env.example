# Database (PostgreSQL)
# For local development, you can use a local PostgreSQL or a free tier from services like Supabase
# For production deployment, this will be automatically set by your hosting provider (Railway, Vercel, etc.)
# DATABASE_URL="postgresql://user:password@localhost:5432/competitor_screenshots?schema=public"
